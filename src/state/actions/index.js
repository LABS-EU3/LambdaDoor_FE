import {
  GetCompanyReviews,
  GetSalaryReviews,
  GetInterviewReviews,
} from './reviews';

// eslint-disable-next-line import/prefer-default-export
export const actions = {
  GetCompanyReviews,
  GetSalaryReviews,
  GetInterviewReviews,
};
